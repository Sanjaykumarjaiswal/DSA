#include<iostream>
// #include<bits/stdc++.h>

#include<string>
using namespace std;

int main(){
    // string t;
    // cin>>t;
    // cout<<s<<endl;

    
    // when we use string to use characters so it doesnot store anything after space.
    //to get rid out of it we use
    // getline(cin,t)

    /*
    string s;
    getline(cin,s);
    cout<<s<<endl;
    cout<<s.length();
    */

    //prints the exact number of characters in s array.

    //Add a character at the end of string s:
    string a="hello";
    a.push_back('w');
    

    //Remove the last character:
    a.push_back('o');
    cout<<a<<endl;

    a.pop_back();
    cout<<a<<endl;

    //if we want to add character in between then
    
    



    




}
